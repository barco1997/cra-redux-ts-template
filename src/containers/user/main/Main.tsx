import React from "react";
// @ts-ignore
import styled from "@xstyled/styled-components";

const Wrapper = styled.div`
  display: flex;
  background: gray;
`;

export const Main = () => {
  return <Wrapper />;
};
