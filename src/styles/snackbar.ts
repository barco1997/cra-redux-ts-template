import { theme } from "./theme";

export const styles = {
  success: { backgroundColor: theme.colors.green },
  error: { backgroundColor: theme.colors.red },
  warning: { backgroundColor: theme.colors.primary },
  info: { backgroundColor: theme.colors.gray },
};
